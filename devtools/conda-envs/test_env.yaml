name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - conda:
    - eigen
    - pybind11

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

